id: custommig
label: MNGL Migration Group
decription: Drupal 7 custom migration group
source_type: Drupal 7
shared_configuration:
  source:
    key: migrate
#above key is the name in settings.php for the D7 database settings
dependencies:
  enforced:
    module:
      - migrate_custombsos
